def fetch_tweets(keyword):
    """Mock function to fetch tweets. Replace with Twitter API calls."""
    return [
        f"{keyword} is leading in ESG innovation! 😊",
        f"Criticism on {keyword} ESG report is rising. 🤨",
        f"{keyword} faces backlash over sustainability claims. 😡"
    ]
